const { getSignerFromPrivateKey } = require('iexec').utils;

const computeOracleKey = () => {
  throw Error('TODO');
};

const testRawParams = () => {
  throw Error('TODO');
};

module.exports = {
  computeOracleKey,
  testRawParams,
  getSignerFromPrivateKey,
};
